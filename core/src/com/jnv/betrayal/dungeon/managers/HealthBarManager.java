package com.jnv.betrayal.dungeon.managers;

import com.badlogic.gdx.scenes.scene2d.actions.Actions;
import com.badlogic.gdx.scenes.scene2d.ui.Image;
import com.jnv.betrayal.gameobjects.Monster;
import com.jnv.betrayal.resources.BetrayalAssetManager;

import javafx.stage.Stage;

public class HealthBarManager {

    private BetrayalAssetManager res;
    private Stage stage;

    public HealthBarManager(BetrayalAssetManager res, Stage stage, int xPos, int yPos){
      /*  int healthBarWidth=250;
        int healthBarHeight=25;
        this.stage = stage;
        image = new Image(res.getTexture("bar"));
        image.layout();
        image.setBounds(xPos, yPos, healthBarWidth, healthBarHeight);
        image.addAction(Actions.alpha(0));
        image.addAction(Actions.delay(1, Actions.fadeIn(2)));
        stage.addActor;

        healthBar= new Image(res.getTexture("green-bar"));
        healthBar.layout();
        healthBar.setBounds(xPos, yPos, healthBarWidth, healthBarHeight);
        healthBar.addAction(Actions.alpha(0));
        healthBar.addAction(Actions.delay(1, Actions.fadeIn(2)));
        healthBar.addActor;
        */
    }
}
